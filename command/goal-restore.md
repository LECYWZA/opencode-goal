---
description: 检查是否有未完成的自动目标任务，询问用户是否继续（跨重启恢复）
---
请先调用 goal_list_incomplete 工具，读取当前持久化的未完成任务（目标、模式、已运行轮数、最近进度、是否暂停），并把清单逐条清楚地展示给用户。

然后询问用户：是否要继续某个任务。
- 若用户指定某条要继续：请调用 goal_set 工具，用该条相同的 goal 和 mode 在当前会话重新激活，并恢复自动运行。
- 若用户说先不管：则不做任何激活，告诉用户目标状态已保留，随时可用 /goal-restore 再恢复。
