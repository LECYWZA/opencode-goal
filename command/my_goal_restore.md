---
description: 检查是否有未完成的自动目标任务，询问用户是否继续（跨重启恢复）
---
请先调用 goal_list_incomplete 工具，读取当前持久化的未完成任务清单，并把每一项清楚展示给用户（目标、模式、已运行轮数、最近进度、是否暂停、当时生效参数）。

然后询问用户：是否要继续某个任务？
- 若用户指定要继续某条：调用 goal_set，用该条相同的 goal、mode 以及展示的生效参数，在当前会话重新激活并恢复自动运行；
- 若用户说先不管：不做激活，告诉用户目标状态已保留，随时可用 /my_goal_restore 再次恢复。
